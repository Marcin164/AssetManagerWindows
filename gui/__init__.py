"""PySide6 GUIs for the LanVentory agent.

Two entry points:

- ``gui.installer`` -- elevated wizard that performs install + initial
  enrollment.
- ``gui.manager`` -- read-only status window with on-demand actions
  (scan now, re-enroll, open log). Runs unprivileged.
"""
